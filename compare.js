const str1 = "hello";
const str2 = "hello";
const str3 = "world";

console.log(str1 === str2); // true, because both strings are identical
console.log(str1 === str3); // false, because the strings are different
